<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.examactivityfragment">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ExamActivityFragment">
        <activity
            android:name=".activity.ShowDataProvider"
            android:exported="false" />

        <provider
            android:name=".provider.ContentProviderP"
            android:authorities="com.example.user"
            android:enabled="true"
            android:exported="true" />

        <activity
            android:name=".activity.Exam2_Provider"
            android:exported="false" />
        <activity
            android:name=".activity.UseInterActivity2"
            android:exported="false" />
        <activity
            android:name=".activity.UseInterActivity"
            android:exported="false" />
        <activity
            android:name=".activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>